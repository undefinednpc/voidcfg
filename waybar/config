{
    "layer": "top",
    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["sway/window"],
    "modules-right": ["battery", "clock", "sway/language", "network"],
    "sway/window": {
        "max-length": 50
    },
    "battery": {
        "format": "{capacity}% {icon}",
        "format-icons": ["", "", "", "", ""]
    },
    "clock": {
        "format-alt": "{:%a, %d. %b  %H:%M}"
    },

    "sway/language": {
	"format": "{}",
    },

    "network": {
	"interface": "wlp2s0",
	"format": "{ifname}",
	"format-wifi": " {essid}",
	"format-ethernet": "{ipaddr}/{cidr}",
	"format-disconnected": "no connection :(",
    }
}
